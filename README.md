# DynamiCode Frontend

React + Tailwind + Vite frontend for DynamiCode.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Create `.env` file based on `.env.example`.
3. Start development server:
   ```bash
   npm run dev
   ```
